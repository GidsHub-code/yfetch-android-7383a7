buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Only used if app/google-services.json is present.
        classpath 'com.google.gms:google-services:4.4.2'
    }
}

plugins {
    id 'com.android.application' version '8.7.3' apply false
}
